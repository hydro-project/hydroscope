/**
 * Deprecated legacy test runner.
 * Tests are executed via Vitest. This file remains as a stub to avoid broken imports.
 */

if (process.env.NODE_ENV !== 'test') {
  console.log('🧪 Tests are executed by Vitest. This legacy runner is no longer used.');
}

export {};
