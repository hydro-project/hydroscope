// --- Layout dimensions ---
export const NODE_WIDTH = 180;
export const NODE_HEIGHT = 40;
export const COLLAPSED_WIDTH = 120;
export const COLLAPSED_HEIGHT = 120;
export const CONTAINER_PADDING_TOP = 30;
export const CONTAINER_PADDING_SIDE = 12;

// --- ELK spacing ---
export const ELK_NODE_SPACING = 30;
export const ELK_LAYER_SPACING = 50;

// --- Semantic zoom ---
export const DOT_ZOOM_THRESHOLD = 0.45;

// --- Auto-expand ---
export const AUTO_EXPAND_AREA_BUDGET = 960_000; // px², roughly 1200×800

// --- Edge markers ---
export const ARROW_SIZE = 16;

// --- Fallback grid ---
export const FALLBACK_COL_SPACING = 220;
export const FALLBACK_ROW_SPACING = 80;
